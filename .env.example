# The site build resolves prompt URLs against this. Leave unset locally and the
# generator falls back to `git describe --tags --abbrev=0`.
BEAMISH_PIN=

# Absolute origin, used for og:image and og:video URLs.
BEAMISH_SITE_ORIGIN=https://beamish.ink
