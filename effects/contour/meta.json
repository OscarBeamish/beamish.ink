{
  "name": "Contour",
  "slug": "contour",
  "tier": 1,
  "description": "A topographic relief on paper, printed with its own contour lines",
  "category": "backdrops",
  "tags": [
    "three",
    "3d",
    "terrain",
    "shader",
    "shadows",
    "map",
    "paper",
    "hero"
  ],
  "peerDependencies": {
    "three": ">=0.160.0"
  },
  "browser": {
    "webgl": "webgl1",
    "notes": [
      "three.js must already be a dependency of the project. It is not bundled",
      "The displacement runs in the vertex shader, so a 160 square grid is one upload and costs memory rather than frame time",
      "The same displacement is injected into a custom depth material. Without that the land casts the shadow of a flat plane and the relief detaches from its own shading"
    ]
  },
  "perf": {
    "frameBudgetMs": 6,
    "contexts": 1,
    "notes": [
      "25,600 vertices, four octaves of noise each, twice: once for the surface and once for the shadow pass",
      "Nothing is uploaded per frame. Only uniforms change"
    ]
  },
  "files": [
    {
      "from": "shared/runtime.ts",
      "to": "src/beamish/shared/runtime.ts",
      "role": "runtime"
    },
    {
      "from": "effects/contour/core.ts",
      "to": "src/beamish/effects/contour/core.ts",
      "role": "core"
    },
    {
      "from": "effects/contour/adapters/react.tsx",
      "to": "src/beamish/effects/contour/react.tsx",
      "role": "adapter"
    },
    {
      "from": "effects/contour/adapters/vue.ts",
      "to": "src/beamish/effects/contour/vue.ts",
      "role": "adapter"
    }
  ],
  "options": {
    "paper": {
      "type": "color",
      "default": "#fbfaf4",
      "description": "The paper behind the land. Match it to your page background or the horizon reads as a hard edge."
    },
    "land": {
      "type": "color",
      "default": "#f4f1e7",
      "description": "The land itself. Keep it close to the paper but not identical, or the horizon disappears entirely."
    },
    "ink": {
      "type": "color",
      "default": "#6f665a",
      "description": "The contour lines. A warm grey reads as print; pure black reads as a wireframe."
    },
    "indexInk": {
      "type": "color",
      "default": "#c44400",
      "description": "Every fifth line is an index contour, the one a real map would label. This is the obvious place for your own colour."
    },
    "relief": {
      "type": "number",
      "default": 1.9,
      "min": 0.2,
      "max": 5,
      "step": 0.05,
      "sensible": [
        1,
        2.4
      ],
      "description": "Height of the land. Past about 3 the ridges are steeper than the sun can light and the far side goes black."
    },
    "scale": {
      "type": "number",
      "default": 0.3,
      "min": 0.1,
      "max": 2,
      "step": 0.01,
      "sensible": [
        0.2,
        0.5
      ],
      "description": "Size of the landforms. Lower is broader country with fewer, longer ridges."
    },
    "density": {
      "type": "number",
      "default": 1.7,
      "min": 0.5,
      "max": 12,
      "step": 0.1,
      "sensible": [
        1.2,
        3
      ],
      "description": "Contour lines per unit of height. More lines reads as steeper country. Past about 8 the lines are closer than the pixels and the map turns grey."
    },
    "weight": {
      "type": "number",
      "default": 0.72,
      "min": 0,
      "max": 1,
      "step": 0.01,
      "sensible": [
        0.45,
        0.85
      ],
      "description": "Weight of the lines. Zero is bare land with no map printed on it, which is a perfectly good backdrop in its own right."
    },
    "elevation": {
      "type": "number",
      "default": 26,
      "min": 5,
      "max": 80,
      "step": 1,
      "unit": "deg",
      "sensible": [
        15,
        40
      ],
      "description": "Sun height above the horizon. Low rakes the ridges and is most of where the depth comes from. High flattens the whole thing into a map."
    },
    "azimuth": {
      "type": "number",
      "default": 38,
      "min": 0,
      "max": 360,
      "step": 1,
      "unit": "deg",
      "sensible": [
        20,
        70
      ],
      "description": "Sun direction around the compass. Convention on a printed map is light from the north west, which is about 315."
    },
    "tilt": {
      "type": "number",
      "default": 0.5,
      "min": 0,
      "max": 1,
      "step": 0.01,
      "sensible": [
        0.3,
        0.7
      ],
      "description": "Camera height. Zero is down on the deck with a horizon, one looks straight down at a map. The interesting ground is in between."
    },
    "zoom": {
      "type": "number",
      "default": 0.55,
      "min": 0.3,
      "max": 2,
      "step": 0.01,
      "sensible": [
        0.45,
        0.8
      ],
      "description": "How much of the frame the land fills."
    },
    "period": {
      "type": "number",
      "default": 6,
      "min": 4,
      "max": 120,
      "step": 1,
      "unit": "s",
      "sensible": [
        6,
        40
      ],
      "description": "Seconds for one loop of the morph. The land travels a closed orbit through noise space and returns exactly. The default is 6 so the preview video is a whole cycle; 20 to 40 is right behind a page, where the land should be moving slowly enough that nobody catches it."
    },
    "reducedMotionTime": {
      "type": "number",
      "default": 3,
      "min": 0,
      "max": 120,
      "step": 0.1,
      "unit": "s",
      "description": "The single frame shown when the user prefers reduced motion. Any time works here: a still relief is a map, which is a finished thing to look at."
    }
  },
  "instruction": "ambient, watch the land breathe",
  "oneShot": false,
  "credit": {
    "what": "real-time terrain with shader displacement",
    "author": "Codrops",
    "url": "https://tympanus.net/codrops/2026/07/22/building-ridgeline-engineering-a-real-time-3d-experience-in-webflow/"
  },
  "record": {
    "duration": 6,
    "fps": 30,
    "posterAt": 2.4,
    "crf": 29,
    "webmCrf": 44
  }
}
